package seedu.recipe.model.cooked;

import seedu.recipe.model.recipe.Recipe;

public class CookedRecord {
    public void addRecord(Recipe recipe) {
    }

    public boolean hasBeenCooked(Recipe recipe) {
    }
}
