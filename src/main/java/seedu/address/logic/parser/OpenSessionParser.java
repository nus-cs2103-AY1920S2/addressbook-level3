package seedu.address.logic.parser;

/**
 * Parse the user input to open a new Session.
 */
public class OpenSessionParser {
    /**
     * Will fill in the gaps once we confirmed the implementation.
     * for switching the classes.
     */
}
